<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">@color/colorPrimary</item>
    </style>

    <style name="Theme.ActivityDialogStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <!--是否半透明-->
        <item name="android:windowIsTranslucent">true</item>
        <!--设置dialog的背景-->
        <item name="android:windowBackground">@color/colorAccent</item>
        <!--背景是否模糊显示-->
        <item name="android:backgroundDimEnabled">true</item>
        <!--设置窗口内容不覆盖-->
        <item name="android:windowContentOverlay">@null</item>
        <!--点击空白地方关闭-->
        <item name="android:windowCloseOnTouchOutside">true</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--设置动画，在这里使用让它继承系统的Animation.Dialog-->
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>

    <style name="test">
        <!--状态栏颜色-->
        <item name="colorPrimaryDark">#00bafe</item>
        <!--控制各个控件被选中时的颜色-->
        <item name="colorAccent">#00bafe</item>
        <!--页面背景色-->
        <item name="android:windowBackground">#00bafe</item>
        <!--api 21 可用 底部导航栏颜色-->
        <item name="android:navigationBarColor">#00bafe</item>
        <!--api 21 可用 Appbar背景色-->
        <item name="android:colorPrimary">#00bafe</item>
        <!--ToolBar上的Title颜色-->
        <item name="android:textColorPrimary">#00bafe</item>
        <!--api 21 可用 各个控制控件的默认颜色-->
        <item name="android:colorControlNormal">#00bafe</item>
    </style>

    <style name="NavigationViewText">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">#444444</item>
<!--        <item name="android:layout_margin">10dp</item>-->
    </style>

    <style name="AppBaseTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="TransluctNavigationTheme" parent="AppTheme">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="BottomPopupTheme">
        <item name="android:windowEnterAnimation">@anim/bottom_pop_enter</item>
        <item name="android:windowExitAnimation">@anim/bottom_pop_exit</item>
    </style>


</resources>